services:
  - type: web
    name: audio-fingerprint-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 120
    envVars:
      - key: DB_PATH
        value: fingerprint_db.json
